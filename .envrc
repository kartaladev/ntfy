# Pins every Go command run in this repository to the toolchain the Makefile and
# CI use, so that golangci-lint and govulncheck built with Go 1.26 keep working
# when a newer Go is first on PATH. Other directories keep their own Go.
export GOTOOLCHAIN=go1.26.8
